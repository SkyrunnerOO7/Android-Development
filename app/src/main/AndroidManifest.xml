<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.crm.pvt.hapinicrm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HapiniCRM">
        <activity android:name=".DeveloperActivity"></activity>
        <activity android:name=".SupportActivity"></activity>
        <activity android:name=".WebViewActivity" />
        <activity android:name=".Error404Activity" />
        <activity android:name=".InternetDialogActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".RegisterdActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

    <!--Main Project | hapiniCRM | Kuldeep Sahu-->
    <!--Change hai bhai-->
    <!--Punit pawar-->
    <!--Rohan -->

</manifest>